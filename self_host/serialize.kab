// SH5: serialize facade — AccAdd body in serialize_sections.
// import "self_host/serialize"
// Re-export by alias (not a wrapping fn) — extra call_value breaks kab-only reentrancy.

import "self_host/serialize_sections"

pub let serialize_bc = serSerializeBc
