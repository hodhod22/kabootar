// SH5: parser facade — parseTokensExec in parser_exec (no parser_impl hop).
// import "self_host/parser"
// Re-export by alias (not a wrapping fn).

import "self_host/parser_exec"

pub let parseTokens = parseTokensExec
