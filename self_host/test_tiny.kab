// Smoke: full compile pipeline
import "self_host/compile"

let tText = compile("let x = 1; return x")
if char_at(tText, 0) != "k" {
    throw "smoke: bad header"
}
if len(tText) < 30 {
    throw "smoke: output too short"
}
println("self_host smoke ok")
