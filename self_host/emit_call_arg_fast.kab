import "self_host/ast_defs"
import "self_host/emit_defs"
import "self_host/emit_hooks"
import "self_host/emit_sym"
import "self_host/emit_sym_index"
import "self_host/emit_op"

let eCallRet = 0

// Fast path: const lit / sym load / full expr for one call argument.
pub fn emitCallArgFast(E, argNode) {
    if argNode.kind == AST_LITERAL {
        eCallRet = symIndex(E, "constLit", argNode)
        E["eIdx"] = eCallRet
        emitOp(E, OP_CONST, eCallRet)
        return 0
    }
    if argNode.kind == AST_VAR {
        E["eSym"] = argNode["sym"]
        emitSym(E, 0, E["eSym"])
        return 0
    }
    eCallExpr(E, argNode)
    return 0
}
