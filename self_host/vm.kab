// SH5: Kab VM facade — exec in vm_run_exec_core (no vm_run/vm_run_body hops).
// import "self_host/vm"
// Re-export by alias (not a wrapping fn) — extra call_value breaks kab-only reentrancy.

import "self_host/vm_run_exec_core"

pub let runModule = runModuleImplBodyCore
