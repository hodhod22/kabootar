// SH5: parse = tokenizeExec + parseTokensExec (no lexer/parser facade hops).
import "self_host/lexer_defs"
import "self_host/lexer_tokenize"
import "self_host/parser_exec"

pub fn parse(source) {
    return parseTokensExec(tokenizeExec(source))
}
