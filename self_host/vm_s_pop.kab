// P6b: pop from session stack.
pub fn vPopS(S) {
    let st = S["stack"]
    if len(st) == 0 {
        throw "vm stack underflow"
    }
    let top = st[len(st) - 1]
    S["stack"] = pop(st)
    return top
}
