// P6b: read fn/module result from S after ops.
pub fn vTakeResult(S) {
    let out = S["result"]
    if S["done"] == false {
        if len(S["stack"]) > 0 {
            out = S["stack"][len(S["stack"]) - 1]
        }
    }
    return out
}
