// SH18/F11: concurrent mark step off gc.kab. import "kab/gc/gc_conc"
// Do not grow gc.kab. One gray object per frame-budget tick. No host GC.

pub fn gcMarkStep(gray, budgetMs) {
    if budgetMs <= 0 {
        return gray
    }
    if gray <= 0 {
        return 0
    }
    return gray - 1
}
