// F10: load persisted first native layout through the host FS capability.
// import "kab/aot/aot_read_layout"; do not import other kab/aot* leaves.

pub fn aotReadLayout(path) {
    return os_read(path)
}
