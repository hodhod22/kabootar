// F10: x64 native images emit a documented jnp opcode; ARM64 has no parity flag.
// import "kab/aot/aot_jnp_op"; do not import other kab/aot* leaves.

pub fn aotJnpOp(target) {
    if target == "x64" {
        return "7b00"
    }
    return ""
}
