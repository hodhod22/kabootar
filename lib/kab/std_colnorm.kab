// SH20: collections L2-norm off std_coldet.kab. import "kab/std_colnorm"
// Do not grow std_coldet.kab. stdHostDeleteOk stays false.

pub fn stdColNorm(a) {
    let s = 0
    let i = 0
    let n = len(a)
    while i < n {
        s = s + a[i] * a[i]
        i = i + 1
    }
    return sqrt(s)
}
