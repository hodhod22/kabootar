// F14/SH24: lookup one header on in-process response. import "kab/http/http_hg"
// No TCP. Do not import other kab/http_* here. No nested calls (overflow).

pub fn httpHeaderOf(code, body, name) {
    let r = http_response(code, body)
    return http_header(r, name)
}
