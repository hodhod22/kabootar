// SH20: collections clone off std_coleq.kab. import "kab/std_colclone"
// Do not grow std_coleq.kab. stdHostDeleteOk stays false.

pub fn stdColClone(p) {
    let out = []
    let n = len(p)
    let i = 0
    while i < n {
        out = push(out, p[i])
        i = i + 1
    }
    return out
}
