// SH20: collections tan off std_colcos.kab. import "kab/std_coltan"
// Do not grow std_colcos.kab. stdHostDeleteOk stays false.

pub fn stdColTan(a) {
    let n = len(a)
    let out = []
    let i = 0
    while i < n {
        out = push(out, tan(a[i]))
        i = i + 1
    }
    return out
}
