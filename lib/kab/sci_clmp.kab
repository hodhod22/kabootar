// SH26: clamp kernel off sci_min.kab. import "kab/sci_clmp"
// Do not grow sci_min.kab. Host GPU remains syscall skuld.

pub fn sciClamp(x, lo, hi) {
    if x < lo {
        return lo
    }
    if x > hi {
        return hi
    }
    return x
}
