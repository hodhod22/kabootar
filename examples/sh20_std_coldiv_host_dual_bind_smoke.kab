import "kab/std/std_coldiv"
import "kab/std/std_colmul"
import "kab/std/std_colfill"
import "kab/std/std_colpush"
import "kab/std/std_host"

if stdHostDeleteOk() != false {
    return false
}
let x = stdColFill(1, 6)
x = stdColPush(x, 0)
let y = stdColFill(1, 7)
y = stdColPush(y, 0)
let p = stdColMul(x, y)
if p[0] != 42 {
    return false
}
let n = stdColFill(1, 84)
n = stdColPush(n, 0)
let den = stdColFill(1, 2)
den = stdColPush(den, 1)
let q = stdColDiv(n, den)
if q[0] != 42 {
    return false
}
return stdHostDeleteOk() == false
