import "kab/std/std_colexpm1"
import "kab/std/std_collog1p"
import "kab/std/std_colfill"
import "kab/std/std_colpush"
import "kab/std/std_host"

if stdHostDeleteOk() != false {
    return false
}
let h = stdColFill(1, 0)
h = stdColPush(h, 0)
let k = stdColLog1p(h)
if k[0] != 0 {
    return false
}
let g = stdColFill(1, 0)
g = stdColPush(g, 0)
let w = stdColExpm1(g)
if w[0] != 0 {
    return false
}
return stdHostDeleteOk() == false
