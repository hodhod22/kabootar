import "kab/sql_insert"
import "kab/sql_host"

if sqlHostDeleteOk() != false {
    return false
}
if sqlIsInsert("INSERT INTO t VALUES (1)") == false {
    return false
}
return sqlHostDeleteOk() == false
