import "kab/ui/ui_divergence"
import "kab/ui/ui_curl"
import "kab/ui/ui_host"

if uiHostDeleteOk() != false {
    return false
}
if uiIsCurl("curl") != true {
    return false
}
if uiIsDivergence("divergence") != true {
    return false
}
return uiHostDeleteOk() == false
