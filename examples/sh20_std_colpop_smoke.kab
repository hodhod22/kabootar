import "kab/std/std_colpop"
import "kab/std/std_colpush"
import "kab/std/std_collen"
import "kab/std/std_col"

let p = stdColPair("n", 42)
p = stdColPush(p, 7)
p = stdColPop(p)
return stdColLen(p) == 2
