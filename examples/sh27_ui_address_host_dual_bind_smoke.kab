import "kab/ui_address"
import "kab/ui_hgroup"
import "kab/ui_host"

if uiHostDeleteOk() != false {
    return false
}
if uiIsHgroup("hgroup") == false {
    return false
}
if uiIsAddress("address") == false {
    return false
}
return uiHostDeleteOk() == false
