import "kab/ui_comment"
import "kab/ui_multicol"
import "kab/ui_host"

if uiHostDeleteOk() != false {
    return false
}
if uiIsMulticol("multicol") == false {
    return false
}
if uiIsComment("comment") == false {
    return false
}
return uiHostDeleteOk() == false
