import "kab/std/std_collog10e"
import "kab/std/std_collog2e"
import "kab/std/std_colfill"
import "kab/std/std_colpush"
import "kab/std/std_host"

if stdHostDeleteOk() != false {
    return false
}
let a = stdColFill(1, 7)
a = stdColPush(a, 0)
let w = stdColLog2e(a)
if w[0] != log2e() {
    return false
}
let k = stdColLog10e(a)
if k[0] != log10e() {
    return false
}
return stdHostDeleteOk() == false
