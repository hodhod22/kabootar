import "kab/aot/aot_load_image"
import "kab/aot/aot_loaded_image"
import "kab/aot/aot_pgo"

if aotPgoOk(8) == false {
    return false
}
let image = aotPgoWarmImage(8, "x64")
if aotLoadedImageOk(image) == false {
    return false
}
return aotLoadImageWithPgoWarmOk(image, "arm64") == false
