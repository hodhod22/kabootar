import "kab/std/std_coldesc"
import "kab/std/std_colfill"
import "kab/std/std_colpush"
import "kab/std/std_coleq"

let p = stdColFill(1, 1)
p = stdColPush(p, 42)
let e = stdColFill(1, 42)
e = stdColPush(e, 1)
return stdColEq(stdColSortDesc(p), e)
