import "kab/http/http_cn"
import "kab/http/http_trc"
import "kab/http/http_host"

if httpHostDeleteOk() != false {
    return false
}
if httpIsTrace("TRACE") == false {
    return false
}
if httpIsConnect("CONNECT") == false {
    return false
}
return httpHostDeleteOk() == false
