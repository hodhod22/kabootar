import "kab/std/std_colatan"
import "kab/std/std_colacos"
import "kab/std/std_colfill"
import "kab/std/std_colpush"
import "kab/std/std_host"

if stdHostDeleteOk() != false {
    return false
}
let h = stdColFill(1, 1.0)
h = stdColPush(h, 0)
let k = stdColAcos(h)
if k[0] != 0 {
    return false
}
let g = stdColFill(1, 0.0)
g = stdColPush(g, 0)
let w = stdColAtan(g)
if w[0] != 0 {
    return false
}
return stdHostDeleteOk() == false
