import "kab/std_coltan"
import "kab/std_colcos"
import "kab/std_colfill"
import "kab/std_colpush"
import "kab/std_host"

if stdHostDeleteOk() != false {
    return false
}
let h = stdColFill(1, 0.0)
h = stdColPush(h, 0)
let k = stdColCos(h)
if k[0] != 1 {
    return false
}
let g = stdColFill(1, 0.0)
g = stdColPush(g, 0)
let w = stdColTan(g)
if w[0] != 0 {
    return false
}
return stdHostDeleteOk() == false
