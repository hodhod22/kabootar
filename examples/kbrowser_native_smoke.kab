// G11 native/host smoke — platform sync + paint (no megabundle).
platform_use("hybrid");
let sync = kb_sync_platform();
let info = kb_os_info();
kb_viewport(1280, 720);
let frame = kb_paint();
print("mode=" + sync.mode + " host=" + info.host_os + " layers=" + len(frame.layers));
