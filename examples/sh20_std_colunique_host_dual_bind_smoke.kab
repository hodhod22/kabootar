import "kab/std_colunique"
import "kab/std_colconcat"
import "kab/std_collen"
import "kab/std_collast"
import "kab/std_col"
import "kab/std_host"

if stdHostDeleteOk() != false {
    return false
}
let p = stdColPair("n", 42)
let d = stdColConcat(p, p)
let u = stdColUnique(d)
if stdColLen(u) != 2 {
    return false
}
if stdColLast(u) != 42 {
    return false
}
return stdHostDeleteOk() == false
