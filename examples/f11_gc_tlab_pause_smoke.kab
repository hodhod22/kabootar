import "kab/gc/gc_host"
import "kab/gc/gc_tlab_pause"
import "kab/gc"

if gcHostDeleteOk() != false {
    return false
}
if gcTlabPauseOk(2, 65536, gcNurseryCap(), gcFrameBudgetMs()) != true {
    return false
}
return gcHostDeleteOk() == false
