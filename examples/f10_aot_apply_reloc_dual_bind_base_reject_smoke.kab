import "kab/aot_apply_reloc"
import "kab/aot_emit_reloc_table"

let table = aotEmitRelocTable("x64")
let addr32 = aotApplyRelocTableAddressAt32(table, -1, "x64")
let addr40 = aotApplyRelocTableAddressAt40(table, -1, "x64")
if addr32 != -1 {
    return false
}
return addr40 == -1
