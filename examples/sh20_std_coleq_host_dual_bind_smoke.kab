import "kab/std/std_coleq"
import "kab/std/std_colunique"
import "kab/std/std_colconcat"
import "kab/std/std_col"
import "kab/std/std_host"

if stdHostDeleteOk() != false {
    return false
}
let p = stdColPair("n", 42)
let u = stdColUnique(stdColConcat(p, p))
if stdColEq(u, p) != true {
    return false
}
if stdColEq(p, p) != true {
    return false
}
return stdHostDeleteOk() == false
