import "kab/std/std_colrshp"
import "kab/std/std_colshape"
import "kab/std/std_colident"
import "kab/std/std_colrange"
import "kab/std/std_colfill"
import "kab/std/std_coleq"
import "kab/std/std_host"

if stdHostDeleteOk() != false {
    return false
}
let es = stdColFill(2, 2)
if stdColEq(stdColShape(stdColIdent(2)), es) == false {
    return false
}
let r = stdColRange(40, 44)
let m = stdColReshape(r, 2, 2)
if stdColEq(stdColShape(m), es) == false {
    return false
}
return stdHostDeleteOk() == false
