import "kab/jit_mmap"
import "kab/jit_exec"
import "kab/jit_np"

pub fn main() {
    let rwx = 7
    let pages = jitPagesFor(6)
    let ok = jitMmapOk(6) && jitExecOk(0) && pages > 0 && rwx == 7
    return ok
}
