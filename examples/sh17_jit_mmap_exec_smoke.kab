import "kab/jit_mmap"
import "kab/jit_np"
import "kab/jit_exec"

let n = 6
if jitMmapOk(n, "rwx") == false {
    return false
}
let pages = jitPagesFor(n)
if pages != 1 {
    return false
}
return jitExecOk(pages, "rwx")
