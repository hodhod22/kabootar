import "kab/std_colchunk"
import "kab/std_colslice"
import "kab/std_colrange"
import "kab/std_coleq"

let r = stdColRange(40, 44)
let c = stdColChunk(r, 2)
if len(c) != 2 {
    return false
}
return stdColEq(c[0], stdColSlice(r, 0, 2))
