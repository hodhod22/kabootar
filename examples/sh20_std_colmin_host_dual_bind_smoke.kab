import "kab/std/std_colmin"
import "kab/std/std_colmax"
import "kab/std/std_colrange"
import "kab/std/std_host"

if stdHostDeleteOk() != false {
    return false
}
let r = stdColRange(40, 43)
if stdColMax(r) != 42 {
    return false
}
if stdColMin(r) != 40 {
    return false
}
return stdHostDeleteOk() == false
